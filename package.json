{
  "name": "dev-and-debate",
  "version": "1.0.0",
  "description": "Dev & Debate - Tech Discussion Platform",
  "scripts": {
    "frontend": "cd frontend && node ./node_modules/next/dist/bin/next dev",
    "backend": "cd backend && node ./node_modules/nodemon/bin/nodemon --exec node ./node_modules/ts-node/dist/bin.js src/index.ts",
    "dev": "start cmd /k \"npm run frontend\" && start cmd /k \"npm run backend\"",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-intersection-observer": "^9.16.0"
  }
}
